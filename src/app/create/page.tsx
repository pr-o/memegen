import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Meme Generator",
  description: "Pick a template and create your meme.",
};

export default function CreatePage() {
  return null;
}
